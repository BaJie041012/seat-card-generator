name: seat_card_app
description: "席卡生成系统 - 本地AI驱动，一键生成会议席卡PDF"
publish_to: 'none'
version: 2.0.2+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  pdf: ^3.11.1
  path_provider: ^2.1.4
  shared_preferences: ^2.3.3
  share_plus: ^10.1.2
  open_filex: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/NotoSansSC-Regular.ttf
